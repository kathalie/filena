import 'package:get_it/get_it.dart';

import '../business/repository_interfaces/folder_repository.dart';
import '../domain/structures/folder_tree.dart';

class FileManagementViewModel {

  // final _isLoading = BehaviorSubject<bool>.seeded(false);
  // Stream<bool> get isLoading => _isLoading.stream;
  //
  // final _error = PublishSubject<String>();
  // Stream<String?> get error => _error.stream;

  // late final BehaviorSubject<FolderTree> folderTree;
  // late final BehaviorSubject<FolderEntity> currentFolder;

  FileManagementViewModel() {
    // folderTree = BehaviorSubject<FolderTree>.seeded();
    // currentFolder = BehaviorSubject<FolderEntity>.seeded();
  }
}