import 'package:flutter/material.dart';

class FilesDisplayMode extends StatelessWidget {
  const FilesDisplayMode({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
